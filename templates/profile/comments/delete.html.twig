{% extends 'base.html.twig' %} {% block title %} Delete comment {% endblock %}{%
block body %} {% include "partials/flash.html.twig" %}

<h1>Delete comment</h1>

<div class="mb-4">
  <p>Comment:</p>
  <p>{{ comment.content | nl2br }}</p>
</div>

<p class="text-danger">
  Are you sure to delete this comment? It can't be recovered afterwards.
</p>

{{ form_start(form) }}
{{ form_row(form.confirmation) }}
<button type="submit" class="btn btn-danger btn-sm">Delete the comment</button>
{{ form_end(form) }}
{% endblock %}
