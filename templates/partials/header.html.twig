<nav class="navbar navbar-expand-lg sticky-top bg-dark text-white p-3 shadow">
  <div class="container-fluid">
    <a class="navbar-brand text-warning" href="{{ path('threads') }}"
      >Threads</a
    >
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon text-white align-center">Menu</span>
    </button>
    <div class="collapse navbar-collapse gap-3" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('threads') }}">Threads</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('threads.create') }}">Create</a>
        </li>
        {% if is_granted('IS_AUTHENTICATED_FULLY') and app.user.threads|length > 0
        %}
        <li class="nav-item">

          <a class="nav-link text-white" href="{{ path('profile.threads') }}"
          >My threads</a
          >
        </li>
        {% endif %} {% if is_granted('IS_AUTHENTICATED_FULLY') and
        app.user.comments|length > 0 %}

        <li class="nav-item">

          <a class="nav-link text-white" href="{{ path('profile.comments') }}"
          >My comments</a
          >
        </li>
        {% endif %}
        
          {% if is_granted('IS_AUTHENTICATED_FULLY') %}
          <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('profile') }}">Profile</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('logout') }}">Logout</a>
          </li>
          {% else %} {% if app.request.requesturi != "/signin" %}
          <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('signin') }}">Sign in</a>
          </li>
          {% endif %} {% if app.request.requesturi != "/signup" %}
          <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('signup') }}">Sign up</a>
          </li>
          {% endif %} {% endif %}
        
      </div>
      </ul>
  </div>
</nav>
